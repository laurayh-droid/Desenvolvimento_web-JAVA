apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-attendance
  labels:
    app: mysql-attendance
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-attendance
  template:
    metadata:
      labels:
        app: mysql-attendance
    spec:
      containers:
        - name: mysql-attendance
          image: mysql:8.0
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: root
            - name: MYSQL_DATABASE
              value: attendance_db
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: mysql-data
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-data
          persistentVolumeClaim:
            claimName: mysql-attendance-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-attendance
spec:
  selector:
    app: mysql-attendance
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
  type: ClusterIP
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-attendance-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
